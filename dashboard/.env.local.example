# Server-only — no NEXT_PUBLIC_ prefix. The browser never talks to the
# backend directly anymore (see lib/server-auth.ts and the /api/proxy
# route), so this URL never ships in the client bundle.
BACKEND_URL=http://localhost:8000
