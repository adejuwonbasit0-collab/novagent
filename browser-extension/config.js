// Change this before packaging for real distribution, or make it
// user-configurable via the popup's settings view (see popup.js).
const NOVA_CONFIG = {
  DEFAULT_BACKEND_URL: "http://localhost:8000",
};
